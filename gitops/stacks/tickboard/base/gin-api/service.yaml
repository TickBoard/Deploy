apiVersion: v1
kind: Service
metadata:
  name: gin-api
  namespace: tickboard
  labels:
    app: gin-api
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /api/health
spec:
  type: ClusterIP
  selector:
    app: gin-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
