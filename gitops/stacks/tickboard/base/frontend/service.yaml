apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: tickboard
  labels:
    app: frontend
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
